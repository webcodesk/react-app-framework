import {
  setDoubleStrings,
  setFirstString,
  setSecondString
} from "usr/api/controlPanelActions";

import exposed from "./exposed";

export default {
  setDoubleStrings,
  setFirstString,
  setSecondString,

  exposed
};
