import {
  firstMethodInChain,
  secondMethodInChain
} from "usr/api/exposed/aboutPanelActions";

import {
  setDoubleStrings,
  setFirstString,
  setSecondString
} from "usr/api/exposed/controlPanelActions";

export default {
  firstMethodInChain,
  secondMethodInChain,

  setDoubleStrings,
  setFirstString,
  setSecondString
};
